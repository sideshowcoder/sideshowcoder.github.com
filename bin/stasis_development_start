#!/bin/bash

CWD=`pwd`
PROJECT_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/../"

echo "starting directory watch, visit http://localhost:3000 to view"
cd $PROJECT_ROOT
bundle exec stasis -d 3000

cd $CWD

